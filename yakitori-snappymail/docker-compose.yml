version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: yakitori-snappymail_web_1
      APP_PORT: 8888

  web:
    image: djmaze/snappymail:latest
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${APP_DATA_DIR}/data:/var/lib/snappymail
